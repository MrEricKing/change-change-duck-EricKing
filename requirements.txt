# ============ 路线啵啵机 · 必装依赖 ============
# 后端框架
flask>=3.0
requests>=2.31

# 地图渲染
folium>=0.15
geopy>=2.4

# 视频下载（抖音 / B 站 / YouTube 等）
yt-dlp>=2024.1

# ffmpeg 二进制（自带，无需系统安装 FFmpeg）
imageio-ffmpeg>=0.5

# ============ 可选依赖（缺失时自动跳过） ============
# 抖音反爬：模拟 Chrome 浏览器指纹
curl-cffi>=0.10,<0.15
# 抖音 cookie 自动获取
browser-cookie3>=0.20

# 关键帧 OCR（识别画面店招/路牌）
rapidocr-onnxruntime>=1.3
pillow>=10.0
opencv-python-headless>=4.8
numpy>=1.24

# 本地 ASR 兜底（优先使用 Whisper API；离线时回退此项）
faster-whisper>=1.0
